# StakeWord 开发计划

> AI驱动的链上目标承诺协议

---

## 一、产品定位

**一句话描述：** 用户向自己的目标押注USDC，Claude全程作为AI教练监督、干预、验证，完成获得本金+利息，失败资金自动分配。

**核心差异化：**
- 押注的标的是**自己**，不是市场
- AI不只是验证者，而是**全程决策的教练**
- 锁定期间资金通过**USYC生息**，惩罚机制变奖励机制
- 履约记录上链，构建**可信行为档案**

---

## 二、核心用户流程

```
1. 连接钱包（Circle Wallets自动创建）
   ↓
2. 描述目标 → Claude对话式细化承诺条款
   ↓
3. 锁定USDC → 自动存入USYC生息
   ↓
4. 履约期：Claude主动推送 + 用户上传证据 + Claude验证
   ↓
5. 结算：
   - 成功 → 本金 + USYC收益 返还
   - 失败 → Claude决定资金去向（慈善/瓜分/继续锁定）
   ↓
6. 履约记录上链，写入用户行为档案
```

---

## 三、功能模块拆解

### 模块1：智能承诺生成器

**Claude在这里做什么：**
- 与用户多轮对话，把模糊目标转化为**可验证的条款**
  - 用户说："我要变健康"
  - Claude追问："多久内？什么频率？如何衡量？"
  - 最终生成："30天内完成20次30分钟以上跑步，每次需上传Strava截图"
- 根据用户历史评估**完成概率**，建议合理押注金额
- 生成正式的**承诺合约文本**（自然语言+结构化数据）

**前端工作：**
- 对话式UI（类似ChatGPT界面）
- 承诺卡片的可视化展示
- 押注金额滑块 + AI推荐值

---

### 模块2：履约期AI教练

**Claude在这里做什么（关键加分项）：**

| 触发条件 | Claude的决策 |
|---------|------------|
| 用户进度落后 | 主动推送鼓励/警告/调整建议 |
| 用户连续完成 | 给予正反馈，可选择"加注" |
| 用户长时间无活动 | 询问遇到的困难，可推荐缩减目标 |
| 验证内容可疑 | 要求补充证据，启动反作弊模式 |
| 接近截止日期 | 计算剩余可行性，给出冲刺策略 |

**前端工作：**
- 进度Dashboard（已完成/剩余/预测完成率）
- 消息通知中心
- 证据上传界面（图片/链接/文本）

---

### 模块3：智能验证系统

**Claude在这里做什么：**
- 视觉分析上传的截图（Strava、Apple Health、读书APP等）
- 提取关键数据（时长、距离、日期）
- 与承诺条款交叉验证
- 动态调整验证严格度（防止作弊模式被学习）
- 生成验证报告并签名上链

**技术要点：**
- 用Claude的视觉能力做截图理解
- 每次验证结果哈希上链（Arc便宜）
- 异常验证触发二次确认流程

---

### 模块4：链上资金管理

**Circle工具使用：**

| 工具 | 用途 |
|------|------|
| **Wallets** | 用户注册自动创建Dev-Controlled Wallet |
| **USDC** | 锁仓资产 |
| **USYC** | 锁定期间生息（核心创新点） |
| **Contracts** | 托管 + 条件释放 + 自动转账 |
| **Paymaster** | 用USDC付gas，用户无感 |
| **Send** | 失败时执行资金转移 |

**合约逻辑：**
- 资金锁定时自动转入USYC
- 验证成功 → 赎回USYC → 返还本金+收益
- 验证失败 → 资金按Claude决策的地址执行Send

---

### 模块5：履约档案与社交层

**Claude在这里做什么：**
- 完成每个承诺后，生成一份**履约总结报告**
- 分析用户的行为模式（坚持力、最佳时段、失败原因）
- 为用户生成可分享的**成就卡片**

**前端工作：**
- 用户主页：累计完成数、总锁仓量、信用分
- 公开榜单：当前进行中的承诺、最近完成的承诺
- 分享卡片设计（推特友好）

**链上数据：**
- 每个承诺的状态变化都有链上记录
- 用户地址 → 履约历史可公开查询

---

## 四、技术架构

### 前端
- **框架：** Next.js + TailwindCSS
- **钱包连接：** Circle Wallets SDK
- **部署：** 自建服务器托管
- **CD：** 后续配置 CD 流水线，自动构建与发布到该服务器

### 后端
- **API层：** Next.js API Routes 或 独立Node服务
- **AI调用：** Anthropic Claude API（含视觉能力）
- **任务队列：** 用于Claude的主动推送任务

### 链上
- **网络：** Arc Testnet（通过Canteen RPC）
- **合约：** 一个核心托管合约 + USYC交互逻辑
- **部署工具：** ARC CLI（已提供）

### 数据存储
- **结构化数据：** Postgres（用户、承诺、履约记录）
- **证据文件：** 对象存储（S3/R2）
- **链上锚定：** 关键验证结果哈希上链

---

## 五、关键技术难点与解决方案

### 难点1：Claude如何"主动"干预？
**解决：** 后端定时任务扫描所有进行中的承诺，根据进度状态调用Claude判断是否需要推送。Claude返回结构化决策（推送/不推送+推送内容）。

### 难点2：截图作弊怎么防？
**解决：**
- Claude视觉分析时检查图片元数据（时间戳、设备信息）
- 同一用户的多次截图做交叉对比（字体、UI版本一致性）
- 关键节点要求录屏或多角度证据
- 不公开具体反作弊规则，让作弊者无法对抗

### 难点3：USYC集成复杂度？
**解决：** Circle已提供USYC的标准接口，直接调用赎回/申购API即可。如果时间紧，先用普通USDC锁仓，USYC作为加分项后期接入。

### 难点4：失败资金去哪？
**解决：** 三种模式让用户在创建承诺时选择：
- 慈善模式：Claude根据目标类型匹配相关公益地址
- 社交模式：分给其他完成同类目标的用户
- 阶梯模式：部分返还+部分销毁，避免完全失去

---

## 六、产品冷启动策略（Traction关键）

1. **创始人自用：** 自己设定3个公开承诺，全程直播
2. **Discord渗透：** 在Canteen Discord发起"黑客松期间完成承诺"挑战
3. **种子用户：** 邀请10个朋友各做一个小额承诺（$1-5）
4. **病毒机制：** 完成后的分享卡片设计成"想跟我打赌吗"的挑战式文案
5. **公开数据看板：** 实时展示总锁仓量、完成率、用户数

---

## 七、Demo与提交准备

### 必备产出
- ✅ Live可用产品（用户能完整跑通流程）
- ✅ 创始人Pitch视频（3-5分钟）
- ✅ GitHub公开仓库
- ✅ 真实用户数据（用户数、锁仓量、完成数）

### Pitch视频结构
1. **30秒** — 演示一个真实承诺从创建到完成的全流程
2. **1分钟** — 解释为什么"押注自己"是预测市场的反转
3. **1分钟** — Claude的Agentic决策展示（不是简单验证）
4. **1分钟** — Circle工具栈的使用，特别是USYC的创新点
5. **30秒** — 真实Traction数据和未来愿景

---

## 八、评分对应自检表

| 评分维度 | 本方案对应实现 |
|---------|------------|
| Agentic 30% | Claude在5个环节做决策：条款生成、进度干预、证据验证、资金去向、行为分析 |
| Traction 30% | 低门槛获客 + 自传播机制 + 公开数据看板 |
| Circle Tool 20% | Wallets/USDC/USYC/Contracts/Paymaster/Send 六个工具 |
| Innovation 20% | 视角反转 + USYC生息惩罚机制 + 链上行为档案 |
